export const MARKDOWN_MODE = "string";
export const PRIMITIVE_EXTRA_TOKENS = 3;
export const KEY_EXTRA_TOKENS = 2; // For `"key":`
export const OBJECT_EXTRA_TOKENS = 2; // For `{}`

export enum MarkdownMode {
  MDAST = "mdast",
  STRING = "string"
}
